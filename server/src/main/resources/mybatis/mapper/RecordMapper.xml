<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.njnu.mapper.RecordMapper">
    <!-- 插入一条知识点浏览记录 -->
    <insert id="addEntityRecord">
        insert into t_browse_record (user_id, browse_date, entity_name) values(#{userID}, #{browseDate}, #{entityName})
    </insert>
    <!-- 插入一条知识点浏览记录 -->
    <insert id="addResourceRecord">
        insert into t_browse_record (user_id, browse_date, resource_id) values(#{userID}, #{browseDate}, #{resourceID})
    </insert>
</mapper>